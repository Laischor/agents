services:
  # Opt-in: set HERMES=1 in .env (compose profile "hermes")
  # Local terminal inside this container (TERMINAL_ENV=local).
  # Gateway API :8642, dashboard :9119.
  hermes:
    profiles: ["hermes"]
    image: nousresearch/hermes-agent:latest
    container_name: hermes
    restart: unless-stopped
    # Do NOT depends_on agents — recreating hermes must not restart live
    # Cursor/Claude sessions in the agents container.
    depends_on:
      - firecrawl
      - searxng
    command: ["gateway", "run"]
    ports:
      - "127.0.0.1:8642:8642"     # gateway API / health
      - "127.0.0.1:9119:9119"     # web dashboard (config / monitoring)
    environment:
      HERMES_DASHBOARD: "1"
      HERMES_DASHBOARD_BASIC_AUTH_USERNAME: ${HERMES_DASHBOARD_USER:-admin}
      HERMES_DASHBOARD_BASIC_AUTH_PASSWORD: ${HERMES_DASHBOARD_PASSWORD:-}
      HERMES_DASHBOARD_BASIC_AUTH_SECRET: ${HERMES_DASHBOARD_SECRET:-}
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: "0.0.0.0"
      API_SERVER_KEY: ${HERMES_API_SERVER_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      # Compose-network URL — Hermes web_search / web_extract (web.backend: firecrawl)
      # Also persisted in data/hermes/.env so get_env_value sees it.
      FIRECRAWL_API_URL: ${FIRECRAWL_API_URL:-http://firecrawl:3002}
      FIRECRAWL_API_KEY: ${FIRECRAWL_API_KEY:-}
      # Compose-network URL — Hermes web_search backend (search_backend: searxng)
      SEARXNG_URL: ${SEARXNG_URL:-http://searxng:8080}
      # macOS: gh stores OAuth in Keychain — start.sh/run.sh inject via `gh auth token`.
      # Hermes Skills Hub / github-auth read GITHUB_TOKEN (GH_TOKEN is the gh CLI name).
      # Local-terminal subprocesses strip GH_TOKEN (Tier-1); git/gh then use the
      # bind-mounted hosts.yml under HOME (see volumes).
      GH_TOKEN: ${GH_TOKEN:-}
      GITHUB_TOKEN: ${GITHUB_TOKEN:-${GH_TOKEN:-}}
      HOST_PROJECTS: ${HOST_PROJECTS}
      # Image default is /opt/data only — also allow write_file/patch under mounted projects
      HERMES_WRITE_SAFE_ROOT: /opt/data:/tmp:${HOST_PROJECTS}
      # Non-root UID for data/hermes bind mount. On macOS use your host id (often 501);
      # start.sh writes HERMES_UID/GID if unset.
      HERMES_UID: ${HERMES_UID:-1000}
      HERMES_GID: ${HERMES_GID:-1000}
      # Shell / file / execute_code run inside this container.
      TERMINAL_ENV: local
    volumes:
      - ./data/hermes:/opt/data
      - ${HOST_PROJECTS}:${HOST_PROJECTS}
      # Host Docker — restart agents / compose from the dashboard when wrap is stuck.
      # Terminal stays local; this is not the old agents:local sandbox backend.
      - ${DOCKER_SOCK:-/var/run/docker.sock}:/var/run/docker.sock
      # Linux gh config with oauth_token (host ~/.config/gh has none — Keychain).
      # Dual mount: hermes passwd HOME is /opt/data; local-terminal subprocesses
      # rewrite HOME to /opt/data/home (home_mode: auto). :ro so stage2's
      # recursive chown of home/ cannot touch host files.
      - ./data/gh:/opt/data/.config/gh:ro
      - ./data/gitconfig:/opt/data/.gitconfig:ro
      - ./data/gh:/opt/data/home/.config/gh:ro
      - ./data/gitconfig:/opt/data/home/.gitconfig:ro
      # Host GitHub SSH — same keys as the agents container.
      - ${HOST_HOME:-${HOME}}/.ssh:/opt/data/.ssh:ro
      - ${HOST_HOME:-${HOME}}/.ssh:/opt/data/home/.ssh:ro

  # Firecrawl-shaped scrape/search API for Hermes web_search / web_extract.
  # One container — official Firecrawl OSS needs Redis+RabbitMQ+Postgres+Playwright.
  # Image: Molx (https://github.com/ioi-labs/molx). Starts with HERMES=1 and/or FIRECRAWL=1.
  firecrawl:
    profiles: ["hermes", "firecrawl"]
    image: ghcr.io/ioi-labs/molx:latest
    container_name: firecrawl
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "127.0.0.1:${FIRECRAWL_PORT:-3002}:3002"
    environment:
      PORT: "3002"
      # Empty = no auth (localhost only). Set FIRECRAWL_API_KEY to require Bearer.
      API_KEY: ${FIRECRAWL_API_KEY:-}
      TIMEOUT_MS: ${FIRECRAWL_TIMEOUT_MS:-60000}
      # Optional LLM extract / onlyCleanContent
      LLM_API_KEY: ${OPENAI_API_KEY:-}
      LLM_BASE_URL: ${OPENAI_BASE_URL:-https://api.openai.com}
      LLM_MODEL: ${FIRECRAWL_MODEL_NAME:-}
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # Metasearch for Hermes web_search (search_backend: searxng) — scrapes the
  # same public engines (startpage, brave, ddg, ...) but server-side, so the
  # container's egress IP gets rate-limited instead of the dev machine's.
  # JSON API enabled via config/searxng/settings.yml.
  searxng:
    profiles: ["hermes", "firecrawl"]
    image: docker.io/searxng/searxng:latest
    container_name: searxng
    restart: unless-stopped
    ports:
      - "127.0.0.1:8888:8080"   # host curl / browser smoke-test
    volumes:
      - ./config/searxng:/etc/searxng:rw
    environment:
      SEARXNG_BASE_URL: http://searxng:8080/
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
      - DAC_OVERRIDE

  agents:
    build:
      context: .
      dockerfile: Dockerfile
    image: agents:local
    container_name: agents
    working_dir: ${HOST_PROJECTS}
    stdin_open: true
    tty: true
    # tini as PID 1 — reap zombies from dead MCP/agent children
    init: true
    # Hard cap so thrash/OOM cannot freeze the whole long-lived container
    # Override via AGENTS_MEM_LIMIT in .env (e.g. 4g, 6g)
    mem_limit: ${AGENTS_MEM_LIMIT:-3g}
    ports:
      - "127.0.0.1:3000:3000"   # wrap — native CLI sessions (tmux / OpenCode API)
    environment:
      # Cursor CLI
      CURSOR_API_KEY: ${CURSOR_API_KEY:-}
      # Pi / LLM providers (set what you use)
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      # GitHub CLI — Keychain on macOS is not visible in Linux; inject at compose time
      GH_TOKEN: ${GH_TOKEN:-}
      # Keep Claude auth/config under one mounted dir (survives rebuild)
      CLAUDE_CONFIG_DIR: /root/.claude
      # Container is root; Claude Code refuses --dangerously-skip-permissions
      # unless it knows it is already sandboxed.
      IS_SANDBOX: "1"
      # Claude caveman SessionStart default (lite|full|ultra|…)
      CAVEMAN_DEFAULT_MODE: ultra
      # Host clipboard-bridge.sh → stubs (xclip/wl-paste) for screenshot paste
      AGENTS_CLIPBOARD_DIR: /var/agents-clipboard
      # Host gpu-bridge.sh → blender/godot shims (Metal on macOS)
      AGENTS_GPU_DIR: /var/agents-gpu
      # Host cmux-bridge.sh → cmux stub (notifications / sounds)
      AGENTS_CMUX_DIR: /var/agents-cmux
      # Dummy display so Cursor CLI enables linux clipboard image paste (xclip stubs)
      DISPLAY: ":0"
      HOST_PROJECTS: ${HOST_PROJECTS}
      # wrap: native Claude/Cursor tmux + OpenCode HTTP (no extra harness)
      AGENTS_WRAP_SERVE: ${AGENTS_WRAP_SERVE:-1}
      WRAP_HOST: "0.0.0.0"
      WRAP_PORT: "3000"
      WRAP_PUBLIC_URL: ${WRAP_PUBLIC_URL:-http://127.0.0.1:3000}
      # Own OpenCode HTTP port — leave the default :4096 free
      WRAP_OPENCODE_PORT: "4097"
      # Hermes as a wrap agent when HERMES=1 (gateway on the compose network)
      HERMES: ${HERMES:-0}
      HERMES_API_SERVER_KEY: ${HERMES_API_SERVER_KEY:-}
      WRAP_HERMES_URL: ${WRAP_HERMES_URL:-http://hermes:8642}
      # Self-hosted Firecrawl / Molx (profile hermes+firecrawl / HERMES=1 or FIRECRAWL=1)
      FIRECRAWL_API_URL: ${FIRECRAWL_API_URL:-http://firecrawl:3002}
    volumes:
      # Same path as host so compose bind mounts resolve correctly
      - ${HOST_PROJECTS}:${HOST_PROJECTS}
      - ${DOCKER_SOCK:-/var/run/docker.sock}:/var/run/docker.sock
      # Host identity for git commits + GitHub SSH / gh CLI
      # Identity only — credential helper + safe.directory live in /etc/gitconfig
      - ${HOME}/.gitconfig:/etc/gitconfig.host:ro
      - ${HOME}/.ssh:/root/.ssh:ro
      - ./data/gh:/root/.config/gh:ro
      # Persist configs on the host (survive rebuild / down -v)
      - ./data/cursor:/root/.cursor
      # Cursor CLI stores OAuth tokens here on Linux (not under ~/.cursor)
      - ./data/cursor-config:/root/.config/cursor
      - ./data/pi:/root/.pi
      - ./data/claude:/root/.claude
      # OpenCode auth/sessions + global config (survive rebuild)
      - ./data/opencode:/root/.local/share/opencode
      - ./data/opencode-config:/root/.config/opencode
      # Live wrap sources (no image rebuild for UI/server tweaks)
      - ./wrap:/usr/local/share/wrap:ro
      - ./bin/wrap-serve.sh:/usr/local/bin/wrap-serve:ro
      # wrap pins / hidden list (survives rebuild)
      - ./data/wrap:/var/lib/wrap
      # macOS clipboard bridge drop dir (PNG for Claude/Cursor/OpenCode Ctrl+V paste)
      - ./data/clipboard:/var/agents-clipboard
      # macOS GPU job queue (Blender/Godot via gpu-bridge.sh)
      - ./data/gpu:/var/agents-gpu
      # macOS cmux notification queue (cmux-bridge.sh)
      - ./data/cmux:/var/agents-cmux
    # Keep the container running for interactive exec
    command: ["sleep", "infinity"]
